# ※ここはbashに依存しないものだけ置く
# 例）環境変数・PATH変数・bin/shで使うものなど

export LANG=ja_JP.utf8
export PATH=$HOME/usr/local/bin:$PATH
export LD_LIBRARY_PATH=$HOME/usr/local/lib:$LD_LIBRARY_PATH
export MANPATH=$HOME/local/share/man:$MANPATH
